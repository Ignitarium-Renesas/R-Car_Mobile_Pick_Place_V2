from setuptools import setup

package_name = 'encoder_msgs'

setup(
    name=package_name,
    version='0.0.1',
    packages=[],
    install_requires=['setuptools'],
    maintainer='user',
    maintainer_email='user@example.com',
    description='Custom message package for encoder values',
    license='Apache-2.0',
    tests_require=['pytest'],
    entry_points={
        'console_scripts': [],
    },
)
